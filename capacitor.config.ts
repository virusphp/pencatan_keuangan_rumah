import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.keluarga.keuangan',
  appName: 'app_keuangan_keluarga',
  webDir: 'build'
};

export default config;
